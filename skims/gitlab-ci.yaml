/deployTerraform/skims:
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  resource_group: $CI_JOB_NAME
  rules:
    - if: $CI_COMMIT_BRANCH != "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /deployTerraform/skims
  stage: deploy-infra
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/dirOfModules/skims/benchmark:
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /lintPython/dirOfModules/skims/benchmark
  stage: lint-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/dirOfModules/skims/cli:
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /lintPython/dirOfModules/skims/cli
  stage: lint-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/dirOfModules/skims/config:
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /lintPython/dirOfModules/skims/config
  stage: lint-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/dirOfModules/skims/core:
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /lintPython/dirOfModules/skims/core
  stage: lint-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/dirOfModules/skims/ctx:
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /lintPython/dirOfModules/skims/ctx
  stage: lint-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/dirOfModules/skims/custom_exceptions:
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /lintPython/dirOfModules/skims/custom_exceptions
  stage: lint-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/dirOfModules/skims/custom_parsers:
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /lintPython/dirOfModules/skims/custom_parsers
  stage: lint-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/dirOfModules/skims/db_model:
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /lintPython/dirOfModules/skims/db_model
  stage: lint-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/dirOfModules/skims/dynamodb:
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /lintPython/dirOfModules/skims/dynamodb
  stage: lint-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/dirOfModules/skims/http_headers:
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /lintPython/dirOfModules/skims/http_headers
  stage: lint-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/dirOfModules/skims/lib:
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /lintPython/dirOfModules/skims/lib
  stage: lint-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/dirOfModules/skims/model:
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /lintPython/dirOfModules/skims/model
  stage: lint-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/dirOfModules/skims/s3:
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /lintPython/dirOfModules/skims/s3
  stage: lint-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/dirOfModules/skims/sast:
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /lintPython/dirOfModules/skims/sast
  stage: lint-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/dirOfModules/skims/sca:
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /lintPython/dirOfModules/skims/sca
  stage: lint-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/dirOfModules/skims/sca_patch:
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /lintPython/dirOfModules/skims/sca_patch
  stage: lint-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/dirOfModules/skims/schedulers:
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /lintPython/dirOfModules/skims/schedulers
  stage: lint-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/dirOfModules/skims/semver_match_tools:
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /lintPython/dirOfModules/skims/semver_match_tools
  stage: lint-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/dirOfModules/skims/serialization:
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /lintPython/dirOfModules/skims/serialization
  stage: lint-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/dirOfModules/skims/state:
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /lintPython/dirOfModules/skims/state
  stage: lint-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/dirOfModules/skims/symbolic_eval:
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /lintPython/dirOfModules/skims/symbolic_eval
  stage: lint-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/dirOfModules/skims/syntax_cfg:
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /lintPython/dirOfModules/skims/syntax_cfg
  stage: lint-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/dirOfModules/skims/syntax_graph:
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /lintPython/dirOfModules/skims/syntax_graph
  stage: lint-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/dirOfModules/skims/test_helpers:
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /lintPython/dirOfModules/skims/test_helpers
  stage: lint-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/dirOfModules/skims/utils:
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /lintPython/dirOfModules/skims/utils
  stage: lint-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/dirOfModules/skims/vulnerabilities:
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /lintPython/dirOfModules/skims/vulnerabilities
  stage: lint-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/dirOfModules/skims/zone:
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /lintPython/dirOfModules/skims/zone
  stage: lint-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/imports/skims:
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /lintPython/imports/skims
  stage: lint-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintPython/module/skimsTest:
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /lintPython/module/skimsTest
  stage: lint-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/lintTerraform/skims:
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /lintTerraform/skims
  stage: lint-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/pipelineOnGitlab/skims:
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /pipelineOnGitlab/skims
  stage: lint-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/securePythonWithBandit/skims:
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /securePythonWithBandit/skims
  stage: lint-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/skims/coverage:
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs:
    - /skims/coverage/combine__1
    - /skims/coverage/combine__2
    - /skims/coverage/combine__3
    - /skims/coverage/combine__4
    - /skims/coverage/combine__5
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /skims/coverage
  stage: post-deploy
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/skims/coverage/combine__1:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs:
    - /testPython/skims@benchmark_cmdi
    - /testPython/skims@benchmark_crypto
    - /testPython/skims@benchmark_hash
    - /testPython/skims@benchmark_ldapi
    - /testPython/skims@benchmark_pathtraver
    - /testPython/skims@benchmark_securecookie
    - /testPython/skims@benchmark_sqli
    - /testPython/skims@benchmark_trustbound
    - /testPython/skims@benchmark_weakrand
    - /testPython/skims@benchmark_xpathi
    - /testPython/skims@benchmark_xss
    - /testPython/skims@f001
    - /testPython/skims@f004
    - /testPython/skims@f005
    - /testPython/skims@f007
    - /testPython/skims@f008
    - /testPython/skims@f009
    - /testPython/skims@f011
    - /testPython/skims@f012
    - /testPython/skims@f015
    - /testPython/skims@f016
    - /testPython/skims@f017
    - /testPython/skims@f021
    - /testPython/skims@f022
    - /testPython/skims@f024
    - /testPython/skims@f031
    - /testPython/skims@f034
    - /testPython/skims@f035
    - /testPython/skims@f037
    - /testPython/skims@f042
    - /testPython/skims@f044
    - /testPython/skims@f052
    - /testPython/skims@f055
    - /testPython/skims@f056
    - /testPython/skims@f058
    - /testPython/skims@f059
    - /testPython/skims@f060
    - /testPython/skims@f063
    - /testPython/skims@f065
    - /testPython/skims@f066
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /skims/coverage/combine '1'
  stage: post-deploy
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/skims/coverage/combine__2:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs:
    - /testPython/skims@f070
    - /testPython/skims@f073
    - /testPython/skims@f075
    - /testPython/skims@f079
    - /testPython/skims@f081
    - /testPython/skims@f083
    - /testPython/skims@f085
    - /testPython/skims@f086
    - /testPython/skims@f089
    - /testPython/skims@f091
    - /testPython/skims@f096
    - /testPython/skims@f097
    - /testPython/skims@f098
    - /testPython/skims@f099
    - /testPython/skims@f100
    - /testPython/skims@f101
    - /testPython/skims@f107
    - /testPython/skims@f109
    - /testPython/skims@f112
    - /testPython/skims@f117
    - /testPython/skims@f120
    - /testPython/skims@f127
    - /testPython/skims@f128
    - /testPython/skims@f130
    - /testPython/skims@f134
    - /testPython/skims@f135
    - /testPython/skims@f143
    - /testPython/skims@f148
    - /testPython/skims@f149
    - /testPython/skims@f152
    - /testPython/skims@f153
    - /testPython/skims@f157
    - /testPython/skims@f160
    - /testPython/skims@f164
    - /testPython/skims@f165
    - /testPython/skims@f169
    - /testPython/skims@f176
    - /testPython/skims@f177
    - /testPython/skims@f183
    - /testPython/skims@f188
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /skims/coverage/combine '2'
  stage: post-deploy
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/skims/coverage/combine__3:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs:
    - /testPython/skims@f200
    - /testPython/skims@f203
    - /testPython/skims@f211
    - /testPython/skims@f234
    - /testPython/skims@f236
    - /testPython/skims@f237
    - /testPython/skims@f239
    - /testPython/skims@f246
    - /testPython/skims@f250
    - /testPython/skims@f256
    - /testPython/skims@f257
    - /testPython/skims@f258
    - /testPython/skims@f259
    - /testPython/skims@f266
    - /testPython/skims@f267
    - /testPython/skims@f277
    - /testPython/skims@f280
    - /testPython/skims@f281
    - /testPython/skims@f297
    - /testPython/skims@f300
    - /testPython/skims@f309
    - /testPython/skims@f313
    - /testPython/skims@f320
    - /testPython/skims@f325
    - /testPython/skims@f332
    - /testPython/skims@f333
    - /testPython/skims@f338
    - /testPython/skims@f335
    - /testPython/skims@f343
    - /testPython/skims@f344
    - /testPython/skims@f346
    - /testPython/skims@f350
    - /testPython/skims@f353
    - /testPython/skims@f354
    - /testPython/skims@f358
    - /testPython/skims@f363
    - /testPython/skims@f366
    - /testPython/skims@f368
    - /testPython/skims@f371
    - /testPython/skims@f372
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /skims/coverage/combine '3'
  stage: post-deploy
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/skims/coverage/combine__4:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs:
    - /testPython/skims@f379
    - /testPython/skims@f380
    - /testPython/skims@f381
    - /testPython/skims@f393
    - /testPython/skims@f394
    - /testPython/skims@f396
    - /testPython/skims@f400
    - /testPython/skims@f401
    - /testPython/skims@f402
    - /testPython/skims@f403
    - /testPython/skims@f405
    - /testPython/skims@f406
    - /testPython/skims@f407
    - /testPython/skims@f408
    - /testPython/skims@f411
    - /testPython/skims@f412
    - /testPython/skims@f413
    - /testPython/skims@f414
    - /testPython/skims@f416
    - /testPython/skims@f418
    - /testPython/skims@f423
    - /testPython/skims@f426
    - /testPython/skims@f427
    - /testPython/skims@f433
    - /testPython/skims@graph_generation
    - /testPython/skims@instance_references
    - /testPython/skims@lib_apk
    - /testPython/skims@lib_http
    - /testPython/skims@lib_http_2
    - /testPython/skims@lib_ssl
    - /testPython/skims@multifile
    - /testPython/skims@nist_c_sharp_f001
    - /testPython/skims@nist_c_sharp_f004
    - /testPython/skims@nist_c_sharp_f008
    - /testPython/skims@nist_c_sharp_f021
    - /testPython/skims@nist_c_sharp_f052
    - /testPython/skims@nist_c_sharp_f063
    - /testPython/skims@nist_c_sharp_f107
    - /testPython/skims@sarif_report
    - /testPython/skims@unittesting
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /skims/coverage/combine '4'
  stage: post-deploy
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/skims/coverage/combine__5:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs:
    - /testPython/skims@vulnerableapp
    - /testPython/skims@vulnerable_js_app
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /skims/coverage/combine '5'
  stage: post-deploy
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@benchmark_cmdi:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@benchmark_cmdi
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@benchmark_crypto:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@benchmark_crypto
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@benchmark_hash:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@benchmark_hash
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@benchmark_ldapi:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@benchmark_ldapi
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@benchmark_pathtraver:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@benchmark_pathtraver
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@benchmark_securecookie:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@benchmark_securecookie
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@benchmark_sqli:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@benchmark_sqli
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@benchmark_trustbound:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@benchmark_trustbound
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@benchmark_weakrand:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@benchmark_weakrand
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@benchmark_xpathi:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@benchmark_xpathi
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@benchmark_xss:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@benchmark_xss
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f001:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f001
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f004:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f004
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f005:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f005
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f007:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f007
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f008:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f008
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f009:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f009
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f011:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f011
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f012:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f012
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f015:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f015
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f016:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f016
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f017:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f017
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f021:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f021
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f022:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f022
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f024:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f024
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f031:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f031
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f034:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f034
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f035:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f035
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f037:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f037
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f042:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f042
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f044:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f044
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f052:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f052
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f055:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f055
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f056:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f056
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f058:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f058
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f059:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f059
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f060:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f060
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f063:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f063
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f065:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f065
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f066:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f066
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f070:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f070
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f073:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f073
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f075:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f075
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f079:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f079
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f081:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f081
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f083:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f083
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f085:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f085
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f086:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f086
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f089:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f089
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f091:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f091
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f096:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f096
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f097:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f097
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f098:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f098
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f099:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f099
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f100:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f100
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f101:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f101
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f107:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f107
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f109:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f109
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f112:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f112
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f117:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f117
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f120:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f120
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f127:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f127
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f128:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f128
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f130:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f130
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f134:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f134
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f135:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f135
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f143:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f143
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f148:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f148
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f149:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f149
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f152:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f152
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f153:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f153
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f157:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f157
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f160:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f160
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f164:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f164
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f165:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f165
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f169:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f169
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f176:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f176
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f177:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f177
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f183:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f183
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f188:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f188
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f200:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f200
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f203:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f203
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f211:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f211
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f234:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f234
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f236:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f236
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f237:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f237
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f239:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f239
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f246:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f246
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f250:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f250
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f256:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f256
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f257:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f257
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f258:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f258
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f259:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f259
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f266:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f266
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f267:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f267
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f277:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f277
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f280:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f280
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f281:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f281
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f297:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f297
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f300:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f300
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f309:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f309
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f313:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f313
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f320:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f320
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f325:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f325
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f332:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f332
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f333:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f333
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f335:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f335
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f338:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f338
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f343:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f343
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f344:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f344
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f346:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f346
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f350:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f350
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f353:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f353
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f354:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f354
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f358:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f358
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f363:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f363
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f366:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f366
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f368:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f368
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f371:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f371
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f372:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f372
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f379:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f379
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f380:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f380
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f381:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f381
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f393:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f393
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f394:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f394
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f396:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f396
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f400:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f400
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f401:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f401
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f402:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f402
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f403:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f403
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f405:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f405
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f406:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f406
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f407:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f407
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f408:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f408
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f411:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f411
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f412:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f412
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f413:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f413
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f414:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f414
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f416:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f416
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f418:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f418
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f423:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f423
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f426:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f426
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f427:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f427
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@f433:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@f433
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@graph_generation:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@graph_generation
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@instance_references:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@instance_references
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@lib_apk:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@lib_apk
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@lib_http:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@lib_http
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@lib_http_2:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@lib_http_2
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@lib_ssl:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@lib_ssl
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@multifile:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@multifile
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@nist_c_sharp_f001:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@nist_c_sharp_f001
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@nist_c_sharp_f004:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@nist_c_sharp_f004
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@nist_c_sharp_f008:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@nist_c_sharp_f008
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@nist_c_sharp_f021:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@nist_c_sharp_f021
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@nist_c_sharp_f052:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@nist_c_sharp_f052
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@nist_c_sharp_f063:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@nist_c_sharp_f063
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@nist_c_sharp_f107:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@nist_c_sharp_f107
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@sarif_report:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@sarif_report
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@unittesting:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@unittesting
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 0
    MAKES_GIT_DEPTH: 0
/testPython/skims@vulnerable_js_app:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@vulnerable_js_app
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testPython/skims@vulnerableapp:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 week
    name: coverage_xml_$CI_COMMIT_REF_NAME_$CI_COMMIT_SHA
    paths:
      - skims/.coverage*
      - provenance-*
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testPython/skims@vulnerableapp
  stage: test-code
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
/testTerraform/skims:
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_COMMIT_BRANCH == "trunk"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|skims)/
  script:
    - m . /testTerraform/skims
  stage: test-infra
  tags:
    - skims-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
