---
lib_root.f309.js_uses_insecure_jwt_token:
  en: Token JWT is not signed with a strong cipher algorithm or not signed at all.
  es: El token JWT no esta firmado con un algoritmo cipher seguro o no se encuentra firmado.
