import styled from "styled-components";

const Container = styled.div.attrs({ className: "tc w-20" })``;

const Icon = styled.div`
  color: #b0b0bf;
  font-size: 2rem;
`;

const Title = styled.div.attrs({ className: "pa3" })``;

const Value = styled.h2``;

export { Container, Icon, Title, Value };
