/integrates/back/test/functional__abandoned_trial_notification:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'abandoned_trial_notification'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__accept_legal:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'accept_legal'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__acknowledge_concurrent_session:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'acknowledge_concurrent_session'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__activate_root:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'activate_root'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__add_access_token:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'add_access_token'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__add_credentials:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'add_credentials'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__add_credit_card_payment_method:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'add_credit_card_payment_method'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__add_enrollment:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'add_enrollment'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__add_event:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'add_event'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__add_event_consult:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'add_event_consult'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__add_files:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'add_files'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__add_files_to_db:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'add_files_to_db'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__add_finding:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'add_finding'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__add_finding_consult:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'add_finding_consult'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__add_forces_execution:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'add_forces_execution'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__add_git_root:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'add_git_root'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__add_group:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'add_group'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__add_group_consult:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'add_group_consult'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__add_group_tags:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'add_group_tags'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__add_ip_root:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'add_ip_root'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__add_organization:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'add_organization'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__add_organization_finding_policy:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'add_organization_finding_policy'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__add_other_payment_method:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'add_other_payment_method'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__add_secret:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'add_secret'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__add_stakeholder:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'add_stakeholder'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__add_toe_input:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'add_toe_input'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__add_toe_lines:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'add_toe_lines'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__add_toe_port:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'add_toe_port'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__add_url_root:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'add_url_root'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__approve_evidence:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'approve_evidence'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__batch:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'batch'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__batch_dispatch:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'batch_dispatch'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__close_vulnerabilities:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'close_vulnerabilities'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__compliance:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'compliance'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__confirm_vulnerabilities:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'confirm_vulnerabilities'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__confirm_vulnerabilities_zero_risk:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'confirm_vulnerabilities_zero_risk'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__consulting_digest_notification:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'consulting_digest_notification'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__deactivate_organization_finding_policy:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'deactivate_organization_finding_policy'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__deactivate_root:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'deactivate_root'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__download_event_file:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'download_event_file'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__download_file:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'download_file'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__download_vulnerability_file:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'download_vulnerability_file'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__drafts_connection:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'drafts_connection'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__event:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'event'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__events:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'events'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__expire_free_trial:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'expire_free_trial'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__finding:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'finding'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__forces_executions:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'forces_executions'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__get_organization_finding_policy:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'get_organization_finding_policy'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__grant_stakeholder_access:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'grant_stakeholder_access'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__grant_stakeholder_organization_access:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'grant_stakeholder_organization_access'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__group:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'group'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__groups_languages_distribution:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'groups_languages_distribution'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__handle_vulnerabilities_acceptance:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'handle_vulnerabilities_acceptance'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__has_access_portfolio:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'has_access_portfolio'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__invalidate_access_token:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'invalidate_access_token'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__machine_queue_all:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'machine_queue_all'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__machine_vulnerability_notification:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'machine_vulnerability_notification'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__me:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'me'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__move_root:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'move_root'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__newsletter_notification:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'newsletter_notification'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__organization:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'organization'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__organization_finding_policy_acceptance:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'organization_finding_policy_acceptance'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__organization_id:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'organization_id'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__organization_vulnerabilities:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'organization_vulnerabilities'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__redirect_cookies:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'redirect_cookies'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__refresh_toe_inputs:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'refresh_toe_inputs'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__refresh_toe_lines:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'refresh_toe_lines'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__refresh_toe_ports:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'refresh_toe_ports'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__reject_event_solution:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'reject_event_solution'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__reject_vulnerabilities:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'reject_vulnerabilities'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__reject_vulnerabilities_zero_risk:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'reject_vulnerabilities_zero_risk'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__remove_credentials:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'remove_credentials'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__remove_event_evidence:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'remove_event_evidence'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__remove_evidence:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'remove_evidence'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__remove_files:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'remove_files'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__remove_finding:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'remove_finding'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__remove_group:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'remove_group'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__remove_group_tag:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'remove_group_tag'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__remove_stakeholder:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'remove_stakeholder'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__remove_stakeholder_access:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'remove_stakeholder_access'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__remove_stakeholder_organization_access:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'remove_stakeholder_organization_access'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__remove_tags:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'remove_tags'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__remove_toe_port:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'remove_toe_port'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__remove_vulnerability:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'remove_vulnerability'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__report:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'report'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__report_machine:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'report_machine'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__report_toe_lines:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'report_toe_lines'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__request_event_verification:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'request_event_verification'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__request_vulnerabilities_hold:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'request_vulnerabilities_hold'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__request_vulnerabilities_verification:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'request_vulnerabilities_verification'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__request_vulnerabilities_zero_risk:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'request_vulnerabilities_zero_risk'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__requeue_actions:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'requeue_actions'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__resources:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'resources'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__resubmit_vulnerabilities:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'resubmit_vulnerabilities'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__reviewer_progress_report:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'reviewer_progress_report'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__root:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'root'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__send_trial_engagement_notification:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'send_trial_engagement_notification'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__solve_event:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'solve_event'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__stakeholder:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'stakeholder'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__streams_process_events:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'streams_process_events'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__streams_process_executions:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'streams_process_executions'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__streams_process_findings:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'streams_process_findings'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__streams_process_lines:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'streams_process_lines'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__streams_process_vulns:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'streams_process_vulns'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__submit_organization_finding_policy:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'submit_organization_finding_policy'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__sync_git_root:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'sync_git_root'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__tag:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'tag'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__toe_inputs:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'toe_inputs'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__toe_lines_connection:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'toe_lines_connection'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__toe_ports:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'toe_ports'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__treatment_alert_notification:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'treatment_alert_notification'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__unfulfilled_standard_report_url:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'unfulfilled_standard_report_url'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__unsubscribe_from_group:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'unsubscribe_from_group'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__update_access_token:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'update_access_token'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__update_compliance:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'update_compliance'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__update_credentials:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'update_credentials'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__update_credit_card_payment_method:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'update_credit_card_payment_method'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__update_description:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'update_description'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__update_event:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'update_event'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__update_event_evidence:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'update_event_evidence'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__update_evidence:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'update_evidence'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__update_evidence_description:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'update_evidence_description'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__update_forces_access_token:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'update_forces_access_token'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__update_git_environments:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'update_git_environments'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__update_git_root:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'update_git_root'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__update_group:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'update_group'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__update_group_access_info:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'update_group_access_info'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__update_group_disambiguation:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'update_group_disambiguation'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__update_group_info:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'update_group_info'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__update_group_managed:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'update_group_managed'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__update_group_policies:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'update_group_policies'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__update_group_stakeholder:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'update_group_stakeholder'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__update_ip_root:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'update_ip_root'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__update_notification_preferences:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'update_notification_preferences'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__update_oauth_credentials_token:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'update_oauth_credentials_token'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__update_organization_policies:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'update_organization_policies'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__update_organization_stakeholder:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'update_organization_stakeholder'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__update_other_payment_method:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'update_other_payment_method'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__update_root_cloning_status:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'update_root_cloning_status'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__update_severity:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'update_severity'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__update_stakeholder_phone:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'update_stakeholder_phone'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__update_toe_input:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'update_toe_input'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__update_toe_lines_attacked_lines:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'update_toe_lines_attacked_lines'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__update_toe_lines_sorts:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'update_toe_lines_sorts'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__update_toe_port:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'update_toe_port'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__update_toe_vulnerabilities:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'update_toe_vulnerabilities'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__update_tours:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'update_tours'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__update_url_root:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'update_url_root'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__update_vulnerabilities_severity:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'update_vulnerabilities_severity'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__update_vulnerabilities_treatment:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'update_vulnerabilities_treatment'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__update_vulnerability_description:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'update_vulnerability_description'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__update_vulnerability_treatment:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'update_vulnerability_treatment'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__upload_file:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'upload_file'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__validate_git_access:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'validate_git_access'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__verify_aws_credentials:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'verify_aws_credentials'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__verify_stakeholder:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'verify_stakeholder'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__verify_vulnerabilities_request:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'verify_vulnerabilities_request'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
/integrates/back/test/functional__vulnerability:
  after_script:
    - cp ~/.cache/makes/provenance-* .
  artifacts:
    expire_in: 1 day
    paths:
      - integrates/.coverage*
      - provenance-*
    when: on_success
  image: ghcr.io/fluidattacks/makes/arm64:latest
  interruptible: true
  needs: []
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_COMMIT_TITLE =~ /^(all|integrates)/
  script:
    - m . /integrates/back/test/functional 'vulnerability'
  stage: test-code
  tags:
    - integrates-small
  variables:
    GIT_DEPTH: 1
    MAKES_GIT_DEPTH: 1
    MAKES_NON_ROOT: 1
